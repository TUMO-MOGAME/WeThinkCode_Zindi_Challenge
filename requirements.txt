# Requirements for the final solution (CatBoost + target encoding + robust thresholds).
# Only libraries actually used to reproduce the 0.4084 submission are listed.
# Open-source only; CPU-only (no GPU). Python 3.10+.

# --- core: needed by the self-contained submission notebook ---
numpy==2.2.6
pandas==2.2.3
scikit-learn==1.5.2
catboost==1.2.10
iterative-stratification==0.1.9

# --- also used by the src/ package CLI reproduction path ---
pyyaml==6.0.3          # reads YAML config
joblib==1.5.2          # saves/loads the fitted model
pyarrow==22.0.0        # parquet IO for OOF / probability files
